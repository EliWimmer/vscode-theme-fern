<div class="container" data-testid="syntax-test"></div>
  <!-- Testing different syntax elements -->
  <h1 style="color: #ff3e00;">Syntax Highlighting Test</h1>

  <script lang="ts">
    const invalidVariable = ;  // This will cause an error
    let unusedVar = "warning"; // This will cause a warning

    function calculateSum(a: number, b: number): number {
      return a + b;
    }
  </script>

  <style>
    .container {
      --custom-prop: #123456;
      background: rgb(240, 240, 240);
      display: flex;
      gap: 1rem;
    }

    @media (max-width: 768px) {
      .container { padding: 1rem; }
    }
  </style>

  <ul>
    <li class="item-{dynamic}" data-value="123">
      {#each items as item}
        <span>{item.text}</span>
      {/each}
    </li>
  </ul>

  <input
    type="text"
    bind:value={someValue}
    on:input={handleInput}
    disabled
  />
</div>
